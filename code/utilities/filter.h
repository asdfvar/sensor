#ifndef APPLY_FILTER_H
#define APPLY_FILTER_H

#include "memory_management.h"

void apply_filter (float *data,
                   int    fill,
                   int    N,
                   MEMORY mem_buffer);

#endif
